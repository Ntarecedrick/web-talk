// import { create } from 'zustand'

// interface User {
//   id: number | string |null, 
//   setUserId:(number: number | string | null)  => void
// }

// export const useStore = create<User>((set) => ({
//   id: null,
//  setUserId: (user) => set(()=> ({user}))
// }))